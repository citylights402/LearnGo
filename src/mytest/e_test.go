package e

var a = "abc"
var b = "ABC"

// func TestE(t *testing.T) {
// if b != UpperCase(a) {
// 	t.Errorf("%s要等于%s", a, b) //打印自定义错误信息

// }
// assert.True(t, b == UpperCase(a), "%s要等于%s", UpperCase(a), b)
// assert.Equal(t, b, UpperCase(a), "%s要等于%s", UpperCase(a), b)
// 	assert.Nil(t, b) //是否为空
// }
